# Pre-Packaged Windows Dependencies

This folder includes pre-packaged windows dependencies that can be used to build vMangos without downloading extra stuff

## Optional dependencies

- curl (must be downloaded via script)
